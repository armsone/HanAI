# HanAI 작업 안내

관련 작업을 시작할 때 상위 공통 규칙과 이 저장소의 `PROJECT_RULES.md`를 끝까지 읽고 따른다.

대표님이 말하는 `한양`은 이 프로젝트 `HanAI`다. 한양의 학습·진화·성능 향상은 기본적으로 HanAI 안에서만 진행하며 HanClip에 즉시 적용하지 않는다.

- 대표님이 `마무리`라고 하면 HanAI 소스 검토와 관련 검증을 끝낸 뒤, 이번 변경만 커밋하고 기본 원격에 푸시한다.
- 대표님이 `릴리즈`라고 하면 그때 검증된 HanAI 기능을 `project-sync`의 `hanclip` 그룹인 HanClip Apple과 HanClip Android에 함께 반영하고 각각 검증한다. 앱스토어·TestFlight·GitHub Releases 같은 공개 배포는 별도 지시가 있어야 한다.
